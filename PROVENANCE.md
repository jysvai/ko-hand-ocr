# ko-hand-ocr 출처 기록 (Provenance)

이 모델이 Apache-2.0 으로 배포될 수 있는 근거를 부품별로 적는다.
"자유롭게 쓸 수 있다더라" 는 근거가 아니다. 각 줄은 확인한 출처가 있어야 한다.

## 1. 왜 ko-trocr 를 안 쓰는가

`ddobokki/ko-trocr` 를 미세조정하면 결과물은 파생 저작물이 되고,
그 학습 데이터인 **AI Hub 한국어 손글씨 데이터셋**의 이용약관을 그대로 물려받는다.
AI Hub 는 국내 이용자 승인·목적 제한이 붙어 있어 무제한 재배포와 맞지 않는다.
따라서 가중치를 물려받지 않고 **처음부터 새로 학습**한다. (clean room)

원래 쓰던 ko-trocr 는 사내 전용 경로에서만 계속 쓴다. 공개 패키지에는 넣지 않는다.

## 2. 글꼴 — 나눔손글씨 109벌 + 구글 OFL 14벌 + 그 밖의 손글씨 329벌

- 만든 곳: NAVER Corporation / CLOVA AI OCR Team, 2019
  (글꼴 파일 name table nameID 0 에 그대로 박혀 있음)
- 공식 안내: https://clova.ai/handwriting
  > "나눔손글씨 글꼴은 기업 및 단체 또는 개인 누구나 무료로 사용할 수 있습니다.
  >  다만, 나눔손글씨 글꼴을 유상으로 제공, 판매, 배포하는 것은 금지됩니다."
- 널리 SIL OFL 1.1 로도 소개되지만, **글꼴 파일 자체에는 라이선스 문구가 없다**
  (109개 전부 nameID 13/14 비어 있음 — 직접 확인함).
  그래서 OFL 재배포 권리에 기대지 않고, 위의 CLOVA 공식 문구만 근거로 삼는다.

### 우리가 지키는 선

두 해석(OFL 이든 CLOVA 문구든) 모두 아래는 확실히 허용한다:

| 하는 것 | 판단 |
|---|---|
| 글꼴로 글자를 그려서 학습 이미지를 만든다 | 허용 — 글꼴의 정상적인 "사용" |
| 그 이미지로 학습한 가중치를 배포한다 | 허용 — 가중치는 글꼴 소프트웨어가 아니다 |
| **글꼴 .ttf 를 저장소/패키지에 넣는다** | **안 함** |
| **글꼴을 팔거나 유상 배포한다** | **안 함** |

글꼴은 저장소에 넣지 않는다. `tools/fetch_fonts.py` 가 학습 전에 내려받고,
학습이 끝나면 그림도 버리고 가중치만 남긴다.

### 구글 OFL 손글씨 14벌 (2026-09-02 더함)

나눔손글씨 109벌은 네이버가 낸 전량이라 더 없다. 손글씨를 읽는 실력은 **본
손씨의 가짓수**가 거의 다 정하므로 밖에서 더 모았다.

- 받는 곳: `google/fonts` 저장소 (`tools/fetch_fonts.py`)
- 라이선스: **SIL Open Font License 1.1** — 나눔손글씨와 달리 이쪽은 글꼴 파일에
  라이선스가 분명히 붙어 있다. 받을 때 `OFL.txt` 가 같이 있는 것만 받는다.
- 받은 것: 감자꽃(GamjaFlower), 개구(Gaegu, Light/Regular/Bold), 귀여운글꼴(CuteFont),
  기랑해랑(KirangHaerang), 나눔브러시스크립트(NanumBrushScript),
  나눔펜스크립트(NanumPenScript), 독도(Dokdo), 동해독도(EastSeaDokdo),
  싱글데이(SingleDay), 연성(YeonSung), 푸어스토리(PoorStory), 하이멜로디(HiMelody)

OFL 은 글꼴 파일을 **다시 배포**하는 것에 조건을 걸지, 그 글꼴로 **그림을 그리는
것**은 막지 않는다. 그러니 나눔손글씨와 **똑같은 선**을 지킨다 — 그림을 그리는
데만 쓰고, 파일은 저장소에도 꾸러미에도 넣지 않는다.

### 그 밖의 손글씨 329벌 (2026-09-17 302벌, 2026-09-18 27벌 더함)

세는 단위는 **디자인(벌)**이다. 파일은 359개인데, 굵기만 다른 같은 디자인이
있어서(예: 학교안심 붓펜 Bold·Light·Medium) 벌로 세면 329다. 목록은
`tools/fonts_extra.json` 에 한 줄이 한 파일이다.

2026-09-18 에 더한 27벌(파일 34개)은 이미 들어와 있는 묶음의 **같은 약관
아래**에 있는 것들이다: 학교안심(KERIS) 15벌(OFL 1.1), HS 시리즈 9벌, 국립공원
꼬미와 추사 사랑체(공공누리 1유형), 인천교육 힘찬체. 주소는 받아지는지 하나씩
확인했다(36개 모두 200 응답).

**가비아 2벌(흘돋·온담)은 일부러 뺐다.** 약관 문구에 "수정 금지" 가 들어 있어
이 저장소의 기준(아래 '뺀 것' 표의 넷째 줄)에 걸린다. 그런데 이미 들어와 있는
가비아 5벌이 같은 약관이라 **그 다섯도 다시 봐야 한다.** 공식 페이지가
자바스크립트라 원문을 아직 못 읽었다.

"손글씨를 읽는 실력은 본 손씨의 가짓수가 정한다"는 같은 까닭으로, 받을 수 있는
한글 손글씨 글꼴을 **모두** 모았다. 후보 386벌을 찾아 주소가 실제로 받아지는지
확인하고, 약관을 벌마다 읽어 302벌을 넣었다(2026-09-18 에 27벌 더해 329벌).
벌마다의 라이선스와 **그것을
어디서 확인했나**는 `tools/fetch_fonts.py` 옆의 `tools/fonts_extra.json` 에 있다.

| 묶음 | 벌 | 라이선스 | 받는 곳 |
|---|---|---|---|
| 학교안심·KCC·한글재민체·나눔바른펜·LXGW 문해 등 | 53 | SIL OFL 1.1 | fonts-archive(GitHub), google/fonts, 저자 GitHub 릴리스 |
| 국립공원 반달이·꼬미, 추사 사랑체 | 3 | 공공누리 1유형 | fonts-archive, 공유마당 등록 페이지 |
| 어비(UhBee) | 93 | 자체 약관: 개인·상업 이용과 수정 허용, 판매 금지 | uhb.kr (공식) |
| 온글잎·그리운·카페24·가비아·HS·교육청 글꼴 등 | 180 | 자체 약관: 상업 이용 허용 | fonts-archive, 눈누 CDN(projectnoonnu) |

**선은 나눔손글씨가 이미 선 자리와 같다.** 나눔손글씨도 OFL 이 아니라 "누구나
무료로 쓰되 유상 배포는 금지"라는 자체 문구다. 그러니 원문에 **상업적 이용까지
허용**한다고 적힌 자체 약관은 같은 급으로 본다. 우리가 하는 일은 여전히 "글꼴로
글자를 그려 학습 그림을 만든다" 하나이고, 글꼴 파일은 어디에도 넣지 않는다.

그래서 **뺀 것**(84벌)은 이렇다.

| 뺀 까닭 | 벌 | 예 |
|---|---|---|
| 반듯한 글꼴 — 손글씨가 아니면 모델이 반듯한 쪽으로 끌린다 | 56 | 학교안심 받아쓰기·몽글몽글, 충주김생체 등 |
| 약관이 **형식 변환·재배포를 금지**하는데 받을 수 있는 것이 웹폰트 사본뿐 | 10 | 교보손글씨 2019~2025 (7), ACC 어린이 (3) |
| 재배포·지정 사이트 밖 배포·임베딩 금지, 또는 허락 필요 | 12 | 밀양 (3), 오방·연일 (3), 사각사각, 어른아이, 광복 이은숙 등 |
| **장평·기울기·왜곡 변형 금지** — 합성이 획을 깎고 기울이므로 지킬 수 없다 | 3 | 잉크립퀴드체, 울산중구전용서체, 달서달링체 |
| 공공누리 3유형·CC-BY-ND (변경 금지) | 3 | 덕온공주체·덕온공주옛체, KCC 박경리체 |

어떤 약관도 기계학습을 따로 적지는 않는다. 이것은 나눔손글씨·구글 OFL 과 같은
사정이고, 위의 "우리가 지키는 선"의 판단을 그대로 따른다.

눈누에만 있는 글꼴은 **웹폰트(woff/woff2)를 바꾸지 않고 그대로** 쓴다.
FreeType 이 직접 읽으므로 ttf 로 변환하지 않는다 — 변환 자체를 금지하는 약관이
있어서 그 길은 아예 안 쓴다.

**시험용 여섯 벌과 같은 디자인은 넣지 않는다.** 이름이 달라도 같은 글꼴이
있다 — 대한민국 독도체는 동해독도, 배민 기랑해랑체는 기랑해랑, 나눔손글씨 펜은
나눔펜스크립트다. 이런 것이 섞이면 '안 배운 글씨체' 점수가 거짓이 된다.
`fetch_fonts.py` 가 받을 때마다 **그림으로** 견주어(같은 글월을 그려 겹치는
넓이, 서로 다른 글꼴은 최대 0.445, 같은 글꼴은 1.0) 0.7 이 넘으면 글꼴 폴더
아래 `twins/` 로 비켜 두고, 학습은 그 폴더를 안 훑는다.

**이 중 여덟 벌은 음절을 다 담지 못한다**(11,172자 중 19.5%). 글꼴에 없는 글자를
그리면 두부(□)가 나오는데, 그것을 손글씨로 배우면 모델이 망가진다. 그래서
`Fonts.pick(rng, text)` 가 **그 글월을 담은 글꼴만** 고른다. 라이선스와는 상관없는
이야기지만, 글꼴을 더 넣으려는 사람이 반드시 걸리는 자리라 여기 적어 둔다.

## 3. 인코더

- 모델: `facebook/deit-small-patch16-224`
- 라이선스: **Apache-2.0** (HuggingFace 모델 카드 태그, 2026-08-31 확인)
- 학습 데이터: ImageNet-1k. 손글씨 데이터가 아니라 일반 사진이라
  AI Hub 같은 국내 데이터셋 약관이 붙지 않는다.
- 21.7M 파라미터. 우리가 쓰는 것은 분류 머리를 뗀 몸통뿐이다.

주의: 이름은 deit 인데 저장된 것은 **vit 체크포인트**다.
`DeiTModel.from_pretrained` 로 열면 가중치가 하나도 안 실리고 조용히 난수로 시작한다
(실제로 겪었다 — LOAD REPORT 에 전부 MISSING 으로 찍힌다).
`ViTModel` 로 열어야 198개가 전부 실린다.

위치 임베딩은 14x14 격자에서 4x40 격자로 늘려 박았다. 이 변형은 우리가 한 것이고
원본 가중치에서 유도한 값이므로 라이선스 관계는 그대로 Apache-2.0 이다.

## 4. 학습 문장

우리가 쓴다. 외부 말뭉치를 가져오지 않는다.
- 자모/영문/숫자 전수 조합 (커버리지용)
- 사내 문서에서 실제로 쓰는 이름·부서·서류명 **형태**만 본뜬 생성 문장
  (실제 사내 명단은 절대 안 들어간다. `data/` 는 이 저장소 밖이다.)

## 5. 사내 손글씨 (2단계)

직원이 직접 쓴 글씨로 하는 화자 적응은 **비공개 전용**이다.
공개 저장소·npm 패키지·모델 허브 어디에도 올리지 않는다.
